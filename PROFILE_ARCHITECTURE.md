# مخطط وهيكلية شاشة ملف المستخدم الشخصي (UserProfileScreen)

تم تطبيق الهيكلية المعمارية لشاشة البروفايل بالكامل والمطابقة لملف `activity_profile.xml` و `ProfileActivity` من تطبيق KissLive (المجلد 2)، وتفعيل التوجيه التلقائي إليها عند الضغط على صورة المستخدم في شاشة "أنا" (Me Tab).

---

## 🏗️ الهيكلية المعمارية المطبقة (Architecture Breakdown)

### 1. سلايدر ألبوم الصور العلوي (`@id/photoBanner`)
- عارض صور تفاعلي (`PageView.builder`) بنسبة أبعاد 1:1 مخصصة لألبوم صور المستخدم وغلافه.
- مؤشرات تنقل نقطية متجاوبة (Dot Page Indicator).
- شريط ملاحة علوي شفاف يضم:
  - زر الرجوع الدائري (`Back Button`).
  - شارة الغرفة الحية (`🔥 في الغرفة`) مع اسم الغرفة وإمكانية الدخول إليها مباشرة إذا كان المستخدم متواجداً بغرفة.
  - زر تعديل / رفع صورة الألبوم للحساب الشخصي.

### 2. بطاقة معلومات وهوية المستخدم (`@id/normalUserInfoLayout` & `@id/sVipGroup`)
- الصورة الشخصية وإطار الـ SVGA النشط (`@id/avatar` مع `SvgaFrame`).
- اسم المستخدم والـ ID مع زر النسخ بلمسة واحدة (`@id/copyBtn`).
- شارة الجنس والعمر (`👦 24` / `👧 22`).
- مستويات السحر، الثراء، والـ VIP الملكية (`@id/nobilityView`).
- مؤشر واجهة الشريك المدمجة (`@id/cpLayout`): عرض صورة المستخدم وشريكه متصلين بقلب CP ينقلك مباشرة لمساحة الـ CP عند النقر.
- مشغل الصوت التعريفي (`@id/voicePlayView`): تشغيل النبذة الصوتية مع أمواج صوتية متحركة.
- شريط الإحصائيات الرباعي: المتابعون، يتابع، الزوار، وإجمالي الهدايا.

### 3. شريط التبويبات الثابت (`@id/magicIndicator` + `TabBarView`)
تم دمج تبويبين رئيسيين (مع استبعاد المنشورات كما طُلب):

#### تبويب 🏆 الشرف والشريك (`ProfileHonorFragment`):
1. **بطاقة علاقة الـ CP**: تعرض صورة الشريكين، مستوى العلاقة، نقاط القربى، عدد الأيام معاً، وزر الدخول لمساحة الـ CP.
2. **بطاقة العائلة / الوكالة**: شعار العائلة، الاسم، كود الكيان، والرتبة مع إمكانية تصفح ملف العائلة.
3. **كبار الداعمين للمستخدم (`contributionRecycler`)**: منصة التتويج للمراتب الثلاث الأولى 🥇🥈🥉 مع صورة كل داعم ونقاط الدعم.
4. **خزينة المركبات والدخوليات (`garageRecycler`)**: استعراض سيارات ومركبات المستخدم مع تشغيل أنيميشن SVGA التلقائي.
5. **جدار الهدايا المستلمة (`giftsRecycler`)**: شبكة تفاعلية بالهدايا المستلمة وأعدادها مع نافذة تفاصيل تفاعلية.
6. **الألقاب والأوسمة والقلائد (`nameplate_recycler`)**: أوسمة وقلائد المستخدم بتأثيراتها المميزة.

#### تبويب 👤 البيانات الشخصية (`ProfileInfoFragment`):
1. **النبذة التعريفية (About Me / Bio)**.
2. **البيانات والموقع**: الدولة، المدينة، اللغة، والعمر.
3. **تاغات الاهتمامات (`interestsTagLayout`)**: بطاقات ملونة لاهتمامات المستخدم (موسيقى، ألعاب، سفر، رياضة...).
4. **تاغات الهدف من الاستخدام (`purposesTagLayout`)**: بطاقات الهدف (تكوين صداقات، دردشة صوتية، ألعاب PK...).

### 4. الشريط التفاعلي السفلي (`@id/footerLay`)
- **عند تصفح حسابك الشخصي**:
  - ✏️ **تعديل الحساب** (Edit Profile).
  - 🎒 **الحقيبة** (Backpack).
- **عند تصفح حساب مستخدم آخر**:
  - ➕ **متابعة / إلغاء المتابعة** (Follow/Unfollow).
  - 💬 **دردشة خاصة** (Direct Chat).
  - 🎁 **إرسال هدية** (Send Gift).
